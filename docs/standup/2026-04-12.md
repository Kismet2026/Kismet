# Standup Update — 2026-04-12

## What I did

### Frontend (Phase 1–8) — complete and deployed
- Built full Next.js frontend on `frontend-dev` branch, covering all 6 backend domains
- **Landing page**: animated logo with orbiting particles, rotating taglines, entrance animations
- **Auth flow**: signup → login (verify page ready, pending backend `POST /auth/confirm`)
- **Onboarding**: profile form + 2×3 photo upload grid (presigned URL → S3)
- **Discovery**: framer-motion swipe cards with BaZi score badges (gold/silver/neutral), inline Pass/Like buttons, match celebration modal
- **Matches**: list with avatars, last message preview, unread badges, relative timestamps
- **Chat**: WebSocket real-time + HTTP polling fallback, optimistic send, icebreaker suggestions for empty conversations
- **Profile**: view/edit, photo grid, email preferences (D5), logout
- **Safety**: report dialog in chat (D4), notification badge on tab bar (D5)
- **Design**: custom dark palette (plum/mauve/cream/amber), Dancing Script logo, Libre Baskerville headings, DM Sans body, Phosphor Icons, mobile-first
- Deployed to **Vercel**: https://frontend-hazel-two-58.vercel.app

### Infra & DevOps
- **CORS fix** ([#94](https://github.com/Kismet2026/Kismet/pull/94)) — added `add_cors_preflight` to `KismetService` for cross-stack imported APIs. Without this, all browser API calls from Vercel were blocked (403 on OPTIONS preflight)
- **D3 route revert fix** ([#97](https://github.com/Kismet2026/Kismet/pull/97)) — PR #89 accidentally reverted #86's route conflict fix. Re-applied `/messages/match/{matchId}` and `/presence/user/{userId}`
- **Resolved D3 PRs** — rebased and merged [#88](https://github.com/Kismet2026/Kismet/pull/88) and [#89](https://github.com/Kismet2026/Kismet/pull/89) which had been sitting with unresolved conflicts
- **Deployed all 6 CDK stacks** — including KismetDomain3 (required deleting ROLLBACK_COMPLETE stack from CloudFormation)
- **WebSocket URL**: `wss://0pnx67jcx3.execute-api.us-east-1.amazonaws.com/dev`
- **CI workflows added**:
  - `.github/workflows/cdk-synth.yml` — auto `cdk synth` on PRs touching `infra/` or `services/`
  - `.github/workflows/unit-tests.yml` — 6 domain parallel unit tests + integration tests
- **D2 architecture diagram**: `docs/system-design/diagrams/domain2-architecture.drawio`

## Today

- E2E testing on deployed environment (2 accounts, full signup → match → chat flow)
- Fix issues found during E2E
- Vercel re-deploy after `POST /auth/confirm` is available

## Blockers

### 🔴 [#95 — `POST /auth/confirm` endpoint missing](https://github.com/Kismet2026/Kismet/issues/95) (@QZGao @kakeikin)
- Cognito sends a verification code on signup, but there's no endpoint to confirm it
- Users sign up successfully but **cannot log in** — status stays `UNCONFIRMED`
- Frontend verify page is built and ready, just needs the backend endpoint
- **This is a launch blocker** — no user can complete registration

## Action items

| Who | What | Priority |
|-----|------|----------|
| **D1** (@QZGao @kakeikin @DoubleSky123) | Implement `POST /auth/confirm` — see [#95](https://github.com/Kismet2026/Kismet/issues/95) for full spec + code examples | 🔴 Blocker |
| **D3** (@ParkerCai @kakeikin @QuanxingLuNEU) | Read the [route revert postmortem](https://github.com/Kismet2026/Kismet/blob/main/docs/postmortem/d3-route-revert-2026-04-12.md). Key takeaways: rebase before merge, resolve conflicts promptly, verify deploy after stack changes | 🟡 Process |
| **Everyone** | CI now runs `cdk synth` on infra PRs — route conflicts and stack issues will be caught before merge | ℹ️ FYI |
